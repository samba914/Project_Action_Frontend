export class Stock {
  constructor(public name: string, public symbol: string, public exchange: string, public assetType: string, public ipoDate: string, public delistingDate: string,public status: string) {
  }
}
